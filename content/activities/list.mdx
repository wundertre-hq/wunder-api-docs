export const meta = {
  title: "List activities",
  description: "Retrieve activities across the workspace.",
};

<MethodBadge method="GET" path="/api-activities" />

## Query parameters

<ParamTable rows={[
  { name: "created_since", type: "string (ISO 8601)", description: "Return only activities created at or after this time." },
  { name: "type", type: "string", description: "Filter by activity type: note, email, call, meeting, tag_added, enrichment, deal_linked." },
  { name: "contact_id", type: "uuid", description: "Filter to one contact." },
  { name: "limit", type: "integer", description: "Default 50, max 200." },
]} title="Query" />

## Example request

```bash
curl -X GET \
  'https://api.wundertreos.com/functions/v1/api-activities?type=note&limit=100' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN'
```

## Response

```json
{
  "data": [
    {
      "id": "a_1a2b3c",
      "type": "note",
      "contact_id": "c8b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "actor_id": "u_1234",
      "body": "Great intro call",
      "metadata": {},
      "created_at": "2026-05-17T13:45:00Z"
    }
  ],
  "pagination": { "limit": 50, "offset": 0, "total": 1 }
}
```
