export const meta = {
  title: "List deals",
  description: "List all deals in your organization with optional filtering.",
};

<MethodBadge method="GET" path="/api-deals" />

<TryIt method="GET" path="/api-deals" />


Requires the `contacts:read` scope. Returns deals across all contacts in the caller's organization. Designed to support Zapier polling triggers for "New deal" and "Updated deal".

To resolve `pipeline_id` and `stage_id` values without asking a user to type UUIDs, call [List pipelines](/pipelines/list) or [List pipeline stages](/pipelines/stages).

## Query parameters

<ParamTable rows={[
  { name: "created_since", type: "string (ISO 8601)", description: "Filter to deals with created_at >= value." },
  { name: "updated_since", type: "string (ISO 8601)", description: "Filter to deals with updated_at >= value." },
  { name: "contact_id", type: "uuid", description: "Only deals linked to this contact." },
  { name: "limit", type: "integer", description: "Default 100, max 200." },
  { name: "offset", type: "integer", description: "Default 0." },
  { name: "order", type: "string", description: "asc or desc on created_at. Default desc (newest first)." },
]} title="Query" />

## Example request

```bash
curl -X GET \
  'https://api.wundertreos.com/functions/v1/api-deals?created_since=2026-05-01T00:00:00Z&limit=50' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN'
```

## Response

```json
{
  "data": [
    {
      "id": "d1e2f3a4-1111-2222-3333-444455556666",
      "org_id": "a1b2c3d4-1111-2222-3333-444455556666",
      "contact_id": "c8b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "company_id": null,
      "pipeline_id": "p1a2b3c4-1111-2222-3333-444455556666",
      "stage_id": "s1a2b3c4-1111-2222-3333-444455556666",
      "title": "Acme — Annual plan",
      "value": 12000,
      "currency": "USD",
      "expected_close_date": "2026-06-30",
      "assigned_to": "u1b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "notes": null,
      "won_at": null,
      "lost_at": null,
      "lost_reason": null,
      "created_at": "2026-05-01T10:00:00Z",
      "updated_at": "2026-05-17T12:00:00Z",
      "created_by": "u1b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c"
    }
  ]
}
```

<SchemaTable rows={[
  { name: "data[].id", type: "uuid" },
  { name: "data[].org_id", type: "uuid" },
  { name: "data[].contact_id", type: "uuid", nullable: true, description: "Linked contact." },
  { name: "data[].company_id", type: "uuid", nullable: true, description: "Linked company." },
  { name: "data[].pipeline_id", type: "uuid", description: "Pipeline this deal lives in. Resolve via [List pipelines](/pipelines/list)." },
  { name: "data[].stage_id", type: "uuid", description: "Current stage within the pipeline. Resolve via [List pipeline stages](/pipelines/stages)." },
  { name: "data[].title", type: "string" },
  { name: "data[].value", type: "number", nullable: true, description: "Monetary value of the deal." },
  { name: "data[].currency", type: "string", nullable: true, description: "ISO 4217 code (e.g. USD)." },
  { name: "data[].expected_close_date", type: "string (ISO 8601 date)", nullable: true },
  { name: "data[].assigned_to", type: "uuid", nullable: true, description: "Owner user ID." },
  { name: "data[].notes", type: "string", nullable: true },
  { name: "data[].won_at", type: "string (ISO 8601)", nullable: true },
  { name: "data[].lost_at", type: "string (ISO 8601)", nullable: true },
  { name: "data[].lost_reason", type: "string", nullable: true },
  { name: "data[].created_at", type: "string (ISO 8601)" },
  { name: "data[].updated_at", type: "string (ISO 8601)" },
  { name: "data[].created_by", type: "uuid", nullable: true },
]} />
