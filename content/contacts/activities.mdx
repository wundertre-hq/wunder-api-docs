export const meta = {
  title: "List activities for a contact",
  description: "Return notes, calls, emails, and other timeline entries for a single contact.",
};

<MethodBadge method="GET" path="/api-contacts/:id/activities" />

## Path parameters

<ParamTable rows={[
  { name: "id", type: "uuid", required: true, description: "The contact ID. Must belong to the caller's organization." },
]} title="Path" />

## Query parameters

<ParamTable rows={[
  { name: "type", type: "string", description: "Filter by activity_type (e.g. note, email, call, meeting, tag_added, enrichment)." },
  { name: "created_since", type: "string (ISO 8601)", description: "Return only activities created at or after this timestamp." },
  { name: "limit", type: "integer", description: "Default 50, max 200." },
]} title="Query" />

Results are sorted by `created_at` descending.

## Example request

```bash
curl -X GET \
  'https://api.wundertreos.com/functions/v1/api-contacts/c8b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c/activities?type=note' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN'
```

## Response

```json
{
  "data": [
    {
      "id": "ac1f3a0b-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "org_id": "a1b2c3d4-1111-2222-3333-444455556666",
      "contact_id": "c8b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "activity_type": "note",
      "description": "Had a great intro call — sending proposal Friday.",
      "metadata": {},
      "performed_by": "u1b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "created_at": "2026-05-17T13:45:00Z"
    }
  ]
}
```

<SchemaTable rows={[
  { name: "data[].id", type: "uuid" },
  { name: "data[].org_id", type: "uuid" },
  { name: "data[].contact_id", type: "uuid" },
  { name: "data[].activity_type", type: "string", description: "Type of activity (note, email, call, meeting, etc.)." },
  { name: "data[].description", type: "string", nullable: true, description: "Human‑readable body or summary." },
  { name: "data[].metadata", type: "object", description: "Type‑specific structured data." },
  { name: "data[].performed_by", type: "uuid", nullable: true, description: "User who performed the activity." },
  { name: "data[].created_at", type: "string (ISO 8601)" },
]} />
