export const meta = {
  title: "List activities for a contact",
  description: "Return notes, events, and timeline entries for a single contact.",
};

<MethodBadge method="GET" path="/api-contacts/:id/activities" />

## Path parameters

<ParamTable rows={[
  { name: "id", type: "uuid", required: true, description: "The contact ID." },
]} title="Path" />

## Query parameters

<ParamTable rows={[
  { name: "type", type: "string", description: "Filter by activity type: note, email, call, meeting, tag_added, enrichment, deal_linked." },
  { name: "limit", type: "integer", description: "Default 50, max 200." },
  { name: "offset", type: "integer", description: "Default 0." },
]} title="Query" />

## Example request

```bash
curl -X GET \
  'https://api.wundertreos.com/functions/v1/api-contacts/c8b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c/activities?type=note' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN'
```

## Response

```json
{
  "data": [
    {
      "id": "a_1a2b3c",
      "type": "note",
      "contact_id": "c8b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "actor_id": "u_1234",
      "body": "Had a great intro call — sending proposal Friday.",
      "metadata": {},
      "created_at": "2026-05-17T13:45:00Z"
    }
  ],
  "pagination": { "limit": 50, "offset": 0, "total": 1 }
}
```
