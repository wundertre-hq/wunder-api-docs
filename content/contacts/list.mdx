export const meta = {
  title: "List contacts",
  description: "Retrieve a paginated list of contacts in your organization, optionally filtered by update time or event type.",
};

<MethodBadge method="GET" path="/api-contacts" />

<TryIt method="GET" path="/api-contacts" query={[{ name: "limit", value: "50" }]} />


Returns contacts in the caller's organization. Designed to support both general listing and Zapier‑style polling triggers.

## Query parameters

<ParamTable rows={[
  { name: "updated_since", type: "string (ISO 8601)", description: "Return only contacts updated at or after this timestamp." },
  { name: "event", type: "string", description: "Optional. Filter by event type for Zapier polling. Accepted values: created, updated, tag_added, enriched. When combined with updated_since, returns only contacts matching that specific change type. When set to created, results are sorted by created_at and combined with updated_since as a created_at filter. When set to enriched, results are filtered to contacts with a non‑null enriched_at and sorted by enriched_at." },
  { name: "limit", type: "integer", description: "Max contacts to return. Default 100, max 200." },
  { name: "offset", type: "integer", description: "Pagination offset. Default 0." },
  { name: "order", type: "string", description: "Optional. Sort direction. Accepted values: asc, desc. Defaults to desc (newest first, as expected by Zapier polling triggers)." },
]} title="Query" />

## Example request

```bash
curl -X GET \
  'https://api.wundertreos.com/functions/v1/api-contacts?limit=50&updated_since=2026-01-01T00:00:00Z' \
  -H 'Authorization: Bearer YOUR_ACCESS_TOKEN'
```

```js
const res = await fetch(
  "https://api.wundertreos.com/functions/v1/api-contacts?limit=50",
  { headers: { Authorization: `Bearer ${token}` } }
);
const { data, meta } = await res.json();
```

## Response

```json
{
  "data": [
    {
      "id": "c8b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c",
      "org_id": "a1b2c3d4-1111-2222-3333-444455556666",
      "first_name": "Jane",
      "last_name": "Doe",
      "email": "jane@example.com",
      "phone": "+15551234567",
      "company": "Acme Inc",
      "company_id": null,
      "job_title": "Head of Growth",
      "industry": "SaaS",
      "address_line1": "100 Market St",
      "address_line2": null,
      "city": "San Francisco",
      "state": "CA",
      "zip": "94105",
      "country": "US",
      "tags": ["lead", "vip"],
      "status": "customer",
      "source": "api",
      "score": 72,
      "notes": null,
      "linkedin_url": "https://linkedin.com/in/janedoe",
      "enriched_at": "2026-05-17T11:02:08Z",
      "enrichment_status": "complete",
      "email_subscribed": true,
      "created_at": "2026-04-12T09:31:22Z",
      "updated_at": "2026-05-17T11:02:08Z",
      "created_by": "u1b1f3a0-4d2e-4f9a-9b1c-2d4e5f6a7b8c"
    }
  ],
  "meta": { "limit": 50, "offset": 0, "count": 1 }
}
```

<SchemaTable rows={[
  { name: "data", type: "Contact[]", description: "Array of contact objects." },
  { name: "data[].id", type: "uuid", description: "Unique contact ID." },
  { name: "data[].org_id", type: "uuid", description: "Organization the contact belongs to." },
  { name: "data[].first_name", type: "string", description: "Given name." },
  { name: "data[].last_name", type: "string", description: "Family name." },
  { name: "data[].email", type: "string", nullable: true, description: "Primary email address." },
  { name: "data[].phone", type: "string", nullable: true, description: "Phone number." },
  { name: "data[].company", type: "string", nullable: true, description: "Free‑text company name." },
  { name: "data[].company_id", type: "uuid", nullable: true, description: "Linked company record, if any." },
  { name: "data[].job_title", type: "string", nullable: true, description: "Job title." },
  { name: "data[].industry", type: "string", nullable: true, description: "Industry." },
  { name: "data[].address_line1", type: "string", nullable: true },
  { name: "data[].address_line2", type: "string", nullable: true },
  { name: "data[].city", type: "string", nullable: true },
  { name: "data[].state", type: "string", nullable: true },
  { name: "data[].zip", type: "string", nullable: true },
  { name: "data[].country", type: "string", nullable: true },
  { name: "data[].tags", type: "string[]", description: "Tags applied to the contact." },
  { name: "data[].status", type: "string", description: "Lifecycle status: new, candidate, contacted, qualified, unqualified, customer, or lost." },
  { name: "data[].source", type: "string", description: "How the contact was created (e.g. api, import, form)." },
  { name: "data[].score", type: "integer", nullable: true, description: "Lead score." },
  { name: "data[].notes", type: "string", nullable: true, description: "Free‑form notes." },
  { name: "data[].linkedin_url", type: "string", nullable: true },
  { name: "data[].enriched_at", type: "string (ISO 8601)", nullable: true, description: "When enrichment last ran." },
  { name: "data[].enrichment_status", type: "string", nullable: true },
  { name: "data[].email_subscribed", type: "boolean" },
  { name: "data[].created_at", type: "string (ISO 8601)" },
  { name: "data[].updated_at", type: "string (ISO 8601)" },
  { name: "data[].created_by", type: "uuid", nullable: true, description: "User who created the contact." },
  { name: "meta.limit", type: "integer" },
  { name: "meta.offset", type: "integer" },
  { name: "meta.count", type: "integer", description: "Number of items returned in the current page. Note: count reflects the number of records in this page, not the total across all pages. Use offset to paginate through results." },
]} />
